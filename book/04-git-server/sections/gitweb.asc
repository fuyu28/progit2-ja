=== GitWeb

(((リポジトリの公開, GitWeb)))(((GitWeb)))
プロジェクトへの基本的な読み書きアクセスと読み取り専用アクセスが設定できたら、シンプルなウェブベースのビジュアライザーも用意したいと思うかもしれません。
Gitには GitWebと呼ばれる CGIスクリプトが付属しており、そのような用途に使われることがあります。

[[gitweb]]
.GitWebのウェブベースユーザーインターフェース
image::images/git-instaweb.png[GitWebのウェブベースユーザーインターフェース, pdfwidth=70%]

プロジェクトに対して GitWebがどのように見えるかを確認したい場合、`lighttpd` や `webrick` のような軽量ウェブサーバーがシステムにインストールされていれば、Gitには一時的なインスタンスを起動するコマンドが用意されています。
Linuxマシンでは `lighttpd` がインストールされていることが多いため、プロジェクトディレクトリで `git instaweb` と入力するだけで起動できるかもしれません。
macOSを使用している場合、Leopardには Rubyがあらかじめインストールされているので、`webrick` が最善の選択肢となるでしょう。
lighttpd以外のハンドラーで `instaweb` を起動するには、`--httpd` オプションを付けて実行します。(((git commands, instaweb)))

[source,console]
----
$ git instaweb --httpd=webrick
[2009-02-21 10:02:21] INFO  WEBrick 1.3.1
[2009-02-21 10:02:21] INFO  ruby 1.8.6 (2008-03-03) [universal-darwin9.0]
----

これによりポート 1234で HTTPDサーバーが起動し、そのページを開いたウェブブラウザーが自動的に立ち上がります。
とても簡単です。
作業が終わってサーバーを停止したい場合は、同じコマンドに `--stop` オプションを付けて実行します。

[source,console]
----
$ git instaweb --httpd=webrick --stop
----

チームや公開しているオープンソースプロジェクトのためにウェブインターフェースを常時稼働させたい場合は、通常のウェブサーバーで CGIスクリプトを配信するよう設定する必要があります。
一部の Linuxディストリビューションには `gitweb` パッケージがあり、`apt` や `dnf` でインストールできる場合があるので、まずそちらを試してみるとよいでしょう。
ここでは GitWebを手動でインストールする手順を簡単に説明します。
まず、GitWebが含まれている Gitのソースコードを取得し、カスタム CGIスクリプトを生成します。

[source,console]
----
$ git clone https://git.kernel.org/pub/scm/git/git.git
$ cd git/
$ make GITWEB_PROJECTROOT="/srv/git" prefix=/usr gitweb
    SUBDIR gitweb
    SUBDIR ../
make[2]: `GIT-VERSION-FILE' is up to date.
    GEN gitweb.cgi
    GEN static/gitweb.js
$ sudo cp -Rf gitweb /var/www/
----

`GITWEB_PROJECTROOT` 変数を使って Gitリポジトリの場所をコマンドに伝える必要がある点に注意してください。
次に、Apacheがこのスクリプトに対して CGIを使用するよう設定します。VirtualHostを追加することで対応できます。

[source,console]
----
<VirtualHost *:80>
    ServerName gitserver
    DocumentRoot /var/www/gitweb
    <Directory /var/www/gitweb>
        Options +ExecCGI +FollowSymLinks +SymLinksIfOwnerMatch
        AllowOverride All
        order allow,deny
        Allow from all
        AddHandler cgi-script cgi
        DirectoryIndex gitweb.cgi
    </Directory>
</VirtualHost>
----

繰り返しになりますが、GitWebは CGIや Perlに対応した任意のウェブサーバーで配信できます。他のものを使いたい場合でも、設定はそれほど難しくないはずです。
これで `http://gitserver/` にアクセスして、オンラインでリポジトリを閲覧できるようになるはずです。
